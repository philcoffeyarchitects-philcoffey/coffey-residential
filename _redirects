# Netlify redirects — preserve SEO authority from the old Squarespace URLs.
# Format: from-path  to-path  status

# ── Top-level pages ───────────────────────────────────────────────────
/home                                       /                  301
/who-we-are                                 /studio            301
/our-services-coffey-residential            /process           301
/press-coffey-residential                   /press             301
/awards-coffey-residential                  /press             301
/awards.html                                /press.html        301
/testimonials-coffey-residential            /studio            301
/contact-us-coffey-residential              /contact           301
/book-a-free-consultation-coffey-residential /contact          301
/portfolio-coffey-residential               /homes             301
/portfolio-coffey-residential-1             /homes             301

# ── Blog → Journal (474 posts, slugs preserved) ──────────────────────
/blog                                       /journal           301
/blog/*                                     /journal/:splat    301

# ── Project pages: slugs already match, so we explicitly catch any
# trailing-slash variants and the one project that's been removed ────
/step-house-canada-water                    /homes             301

# ── Other Squarespace pages with no equivalent on the new site ───────
/privacy-policy                             /                  301

# ── Forced redirects (override any matching file) ────────────────────
# `301!` is Netlify's force-redirect flag — applies even if a file
# exists at the source path. Use for legacy short slugs / typo'd URLs
# that need to win over any accidental matching asset.
/portfolio                                  /homes                       301!
/about-us                                   /studio                      301!
/contactus                                  /contact                     301!
/themoderndetached                          /modern-detached-harpenden   301!

# Pretty URLs already work for the new project pages because Netlify
# serves /[slug].html when /[slug] is requested.
